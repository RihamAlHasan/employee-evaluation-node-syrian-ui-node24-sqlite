const router = require('express').Router();
const { requireAuth, requireRole, flash } = require('../middleware');
const { service } = require('../appContext');
const { UserRole, EvaluationStatus } = require('../domain/enums');
router.use(requireAuth);
router.get('/me', (req, res, next) => { try { if (req.session.user.role === UserRole.CentralEvaluationManager) throw new Error('مسؤول التقييم المركزي لا يملك صفحة نتيجتي'); const cycle = service.cycles().find(c=>c.isActive && c.isPublished) || service.cycles()[0]; const result = service.resultForEmployee(req.session.user, cycle.id, req.session.user.id); res.render('reports/me', { title: 'نتيجتي', cycle, result }); } catch(e){ next(e); } });
router.get('/', requireRole(UserRole.Admin, UserRole.CentralEvaluationManager, UserRole.EntityEvaluationManager), (req, res, next) => { try { const cycleId = req.query.cycleId || service.cycles()[0]?.id; const rows = cycleId ? service.reportRows(req.session.user, cycleId) : []; res.render('reports/index', { title: 'التقارير والاعتماد', rows, cycles: service.cycles(), cycleId, statuses: EvaluationStatus }); } catch(e){ next(e); } });
router.post('/process', requireRole(UserRole.Admin, UserRole.CentralEvaluationManager), (req, res, next) => { try { service.processResult(req.session.user, req.body.cycleId, req.body.employeeId, req.body); flash(req,'success','تمت معالجة النتيجة'); res.redirect('/reports?cycleId=' + req.body.cycleId); } catch(e){ next(e); } });
router.post('/approve', requireRole(UserRole.Admin, UserRole.CentralEvaluationManager), (req, res, next) => { try { service.approveResult(req.session.user, req.body.cycleId, req.body.employeeId, req.body); flash(req,'success','تم اعتماد النتيجة'); res.redirect('/reports?cycleId=' + req.body.cycleId); } catch(e){ next(e); } });
router.post('/bulk', requireRole(UserRole.Admin, UserRole.CentralEvaluationManager), (req, res, next) => { try { const ids = Array.isArray(req.body.employeeIds) ? req.body.employeeIds : [req.body.employeeIds].filter(Boolean); let count = 0; for (const employeeId of ids) { const row = service.resultForEmployee(req.session.user, req.body.cycleId, employeeId); if (row.evaluations.some(e => e.status === EvaluationStatus.Processed) && row.adjustment?.resultStatus !== EvaluationStatus.Approved) { if (req.body.action === 'approve') service.approveResult(req.session.user, req.body.cycleId, employeeId, {}); else service.processResult(req.session.user, req.body.cycleId, employeeId, {}); count++; } } flash(req,'success',`تم تنفيذ الإجراء على ${count} نتيجة`); res.redirect('/reports?cycleId=' + req.body.cycleId); } catch(e){ next(e); } });
router.post('/approve-bulk', requireRole(UserRole.Admin, UserRole.CentralEvaluationManager), (req, res, next) => { try { const ids = Array.isArray(req.body.employeeIds) ? req.body.employeeIds : [req.body.employeeIds].filter(Boolean); let count = 0; for (const employeeId of ids) { const row = service.resultForEmployee(req.session.user, req.body.cycleId, employeeId); if (row.evaluations.some(e => e.status === EvaluationStatus.Processed) && row.adjustment?.resultStatus !== EvaluationStatus.Approved) { service.approveResult(req.session.user, req.body.cycleId, employeeId, {}); count++; } } flash(req,'success',`تم اعتماد ${count} نتيجة`); res.redirect('/reports?cycleId=' + req.body.cycleId); } catch(e){ next(e); } });
module.exports = router;
